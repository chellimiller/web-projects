# `@chellimiller/result-api`

This is a TypeScript API for a result that is returned from a hook, promise, subscribable source, or any function.

This should only be used as a dev dependency in a web app.
